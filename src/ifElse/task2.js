// const a = 5;
// const b = 2;
// const c = 4;

function maximal (a,b,c){
    if ((a * b * c) > (a + b + c)){
        return (mult);
        }
    if ((a + b + c) > (a * b * c)){
        return (summ);
        }
    }
 const mult = (a * b * c + 3);
const summ = (a + b + c + 3);
   
console.log(maximal(a,b,c));